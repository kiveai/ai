# Skills

Agent skills that steer AI agents to get great results with Kive. They follow the [Agent Skills specification](https://agentskills.io/specification): each skill is a folder with a `SKILL.md` (what + when in the frontmatter, instructions in the body) and optional `references/` loaded on demand.

This folder is the **source of truth**. The copies inside `providers/*/plugin/skills/` are generated by [`scripts/sync.js`](../scripts/sync.js) — edit here, then run the sync.

We keep the catalog deliberately small: a few skills that each own one workflow beat a large overlapping set (agents pick skills by description, and discovery quality drops as catalogs grow).

Install into your project with:

```bash
npx skills add kiveai/ai
```
